# Full URL of the dictionary API, or leave empty to use bundled public/defects.json on first load.
# VITE_DICTIONARY_URL=http://somedomain.test:8080/dictionary

# Optional: URL to EXR environment map for 3D preview (e.g. /venice_sunset_sky.exr). Leave unset to use fallback lighting.
# VITE_ENVIRONMENT_MAP_URL=/venice_sunset_sky.exr
